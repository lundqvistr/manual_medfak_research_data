name: Publicera Quarto

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Dölj QMD-filer för att stoppa R-motorn
        run: |
          # Vi ändrar filändelsen helt på servern så Quarto bara ser textfiler (.md)
          for f in *.qmd; do
            if [ -f "$f" ]; then
              mv "$f" "${f%.qmd}.md"
            fi
          done

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Bygg dokumentet
        run: |
          quarto render

      - name: Publicera till GitHub Pages via gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          publish_branch: gh-pages
